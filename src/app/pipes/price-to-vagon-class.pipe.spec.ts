import { PriceToVagonClassPipe } from './price-to-vagon-class.pipe';

describe('PriceToVagonClassPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceToVagonClassPipe();
    expect(pipe).toBeTruthy();
  });
});
