import { SplitSeatNumberPipe } from './split-seat-number.pipe';

describe('SplitSeatNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitSeatNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
